export class OneEuro{constructor(private minCutoff=1.0,private beta=0.007,private dcutoff=1.0){} private xPrev:number|null=null; private dxPrev:number|null=null; private tPrev:number|null=null; private alpha(c:number,dt:number){const tau=1/(2*Math.PI*c);return 1/(1+tau/dt)} filter(x:number,t:number){if(this.tPrev==null){this.tPrev=t;this.xPrev=x;this.dxPrev=0;return x} const dt=Math.max(1e-3,t-(this.tPrev||t)); this.tPrev=t; const dx=(x-(this.xPrev||x))/dt; const aD=this.alpha(this.dcutoff,dt); const dxHat=aD*dx+(1-aD)*(this.dxPrev||0); const cutoff=this.minCutoff+this.beta*Math.abs(dxHat); const a=this.alpha(cutoff,dt); const xHat=a*x+(1-a)*(this.xPrev||x); this.xPrev=xHat; this.dxPrev=dxHat; return xHat}}
