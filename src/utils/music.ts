export const TONICS=["C","C#","D","D#","E","F","F#","G","G#","A","A#","B"] as const;export const SCALES:{[k:string]:number[]}={major:[0,2,4,5,7,9,11],minor:[0,2,3,5,7,8,10],pentatonic:[0,2,4,7,9],dorian:[0,2,3,5,7,9,10],mixolydian:[0,2,4,5,7,9,10]};export function quantizeMidi(m:number,t:number,s:string){const sc=SCALES[s]||SCALES.major;const o=Math.floor(m/12);let best=m,bd=999;for(let k=-1;k<=1;k++){for(const d of sc){const c=o*12+k*12+t+d;const dst=Math.abs(c-m);if(dst<bd){best=c;bd=dst}}}return best}export function yToMidi(y:number,lo:number,hi:number){return Math.round(lo+(1-y)*(hi-lo))}
